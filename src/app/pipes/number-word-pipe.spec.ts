import { NumberWordPipe } from './number-word-pipe';

describe('NumberWordPipe', () => {
    it('create an instance', () => {
        const pipe = new NumberWordPipe();
        expect(pipe).toBeTruthy();
    });
});
